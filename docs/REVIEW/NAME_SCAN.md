# Name Scan — publication safety audit

**Result: ZERO hits.** The scan runs against a **fresh clone of the public remote** (not the working directory), so it verifies what is actually published.

- **Scan date:** 26 Aug 2026
- **Public remote:** github.com/wryszka/hb-renewal-workbench
- **Scanned commit SHA:** `5b2cf807180300f8d9e49b32c564243197b51e29` (single commit — history is one commit). The only change in the current HEAD relative to the scanned commit is this recorded SHA line — no code, data, or config changed, so the scan result holds for HEAD.
- **Term source:** the forbidden terms live in a local, gitignored file `.scanlist.local` (in `.gitignore`); they are **not** reproduced in this committed document, so the scan doc itself introduces no real names.

## Categories scanned (terms not listed here by design)

1. Real US medical-insurance **carrier** brand names.
2. The real **brokerage** name associated with the engagement.
3. **Personal names** of individuals named in the source materials (broker, carrier, internal contacts).
4. Real **employer / client** identifiers from the confidential exhibit.
5. Old **confidential filename** patterns (the pre-sanitisation reference/model-spec names).

Each term is matched case-insensitively against: file **contents**, **filenames**, and **commit messages** of the fresh clone.

## What was scanned

- **A fresh `git clone`** of the public remote (single commit; 40 tracked files).
- **Confidential-file absence** across the working tree **and every history object** (`git rev-list --all --objects`): `reference_exhibit.json`, corpus/planning docs, transcripts, the original `MODEL_SPEC` — asserted absent.
- **Secrets:** GitHub / Databricks personal-access-token prefixes, Slack token prefixes, PEM private-key headers, and non-generic emails.
- **Deployed data values:** distinct values of every string column in the business tables (carrier, employer_group, file_name, narratives, audit detail, etc.).

## Findings and fixes

| Finding | Disposition |
|---------|-------------|
| Zero real-party-name hits in contents, filenames, or commit messages | — |
| No confidential files in the tree or in git history | — |
| No token/secret strings | — |
| **A personal email hardcoded as a Genie `parent_path`** (in the old create-only script) | **Fixed** — parent path now derived from the current user at runtime; the public history was **rewritten** to a single sanitised commit (force-push), not layered with a removal commit (per Phase 1.5). Verified absent from a subsequent fresh clone. |
| Emails remaining in the tree | Only generic/synthetic: `analyst@broker.example` (seed actor). |
| Substring false positives ("Ricks" inside "data**bricks**"; `MODEL_SPEC` in intentional doc references) | Not leaks — confirmed via word-boundary re-scan; the `renewal_engine.py` comment reference was updated to `docs/METHOD_SPEC.md`. |
| **A bare engagement first name in `app/selffunded.py` comments** (present since the first public push) | **Fixed 2026-08-24.** Earlier scans matched the full name and missed the bare first name. The scan now includes **bare engagement first names** (word-boundary); the comments were rewritten to generic broker language and the public history rewritten to a sanitised commit. A persona-header shorthand using engagement first names/initials, introduced in `PERSONA_REVIEW_PACK.md`, was scrubbed to role-only in the same pass. |

## Accepted-public (explicit, logged in `DECISIONS.md` D13)

Not secrets — access requires authenticated, granted principals, and these were already in the README: workspace host `fevm-lr-dev-aws-us`, catalog `lr_dev_aws_us_catalog`, warehouse `a3b61648ea4809e3`, the Genie space id, and the dashboard id.

## Deployed-values scan

Distinct carriers in the deployed schema are exactly the five synthetic ones (Summit Health, Ridgeline Mutual, Cascade Care, Evergreen Health, Meridian Assurance); employers are all fictional (Harborview Logistics, Alderman Freight, …). **0 hits.**

## No `.xlsx` in the clone

All `.xlsx` are gitignored (the local reference workbook and the synthetic demo files). Nothing to dump via openpyxl in the published tree; the synthetic demo files are regenerated by `data/make_carrier_file.py`.
