# Build decisions log — HB Renewal Workbench

Running log for the 24 Aug 2026 demo build. Newest first.

## Accepted at kickoff (from spec §7 + hard rules)
- **D1** Idempotent schema rebuild (drop-and-recreate); existing object names preserved.
- **D2** Reviewer agent pre-computed by default; FMAPI path behind `LIVE_REVIEWER` env flag.
- **D3** Genie agent created/updated programmatically via the Genie Agents Management API at deploy; embedded in-app via the Conversation API ("Ask the book" panel).
- **D4** Reference exhibit renamed `data/reference_exhibit.json` + gitignored; sanitised `docs/METHOD_SPEC.md` replaces `MODEL_SPEC.md` in-repo.
- **D5** Hero carrier **Meridian Assurance**, hero employer **Harborview Logistics**, period **2026H2**. Synthetic carriers only (Summit Health, Ridgeline Mutual, Cascade Care, Evergreen Health, Meridian Assurance).
- **D6** App name/URL unchanged: `hb-renewal-workbench`.

## Post-review pass (26 Aug 2026) — newest first
- **D29** Pre-demo (90 min out) we **froze the 3-WP build** (self-funded / quarantine-resolution / version-diff) — too risky to build+deploy+reseed before a live demo, and the Reset button re-runs seed code so a half-deployed bundle could break beats mid-demo. Instead shipped **one low-risk, frontend-only addition: a `Roadmap` panel** (pinned bottom of the sidebar, `renderRoadmap`) listing what's specced/planned but not yet built (the 3 WPs + V2/V3 items) — no pipeline/seed/schema/Reset-path touch; JS brace-balance + local boot verified before deploy; app stayed RUNNING. Mirrored into `docs/DEMO_ROADMAP.md` and appended to the demo Google Doc. The 3-WP build proceeds after the demo from the clean rollback point.
- **D28** `docs/REVIEW/PERSONA_REVIEW_PACK.md` — single self-contained app+demo description for external persona review (surface census · keystroke click-flows · state machine · persona interrogation · numbers appendix · ranked weaknesses register). Verified against a fresh Reset (not memory). Surfaced two real defects to fix or narrate: (i) the **Credibility (experience) lever** — JS preview auto-balances weights to sum 1, but `save_scenario` sends only the changed weight to the UC function, so the saved value keeps the original manual weight (weights ≠ 1, saved ≠ preview); (ii) the **lineage panel shows the current table version, not the compute-time version** (`DESCRIBE HISTORY … LIMIT 1`, `main.py:209`). Also documented: value-negotiated reconciles across all four surfaces (~$12.975M); no edit/delete/approve of a saved scenario; FMAPI stays in-workspace; inputs escaped via `sq()` not parameterised. Linked from README.
- **D27** Benchmarks rows are now **clickable → lineage** (the docs claimed "click a row's lineage" but nothing happened). New `GET /api/benchmark/scenarios?carrier=&band=` returns the retained decisions behind a cell; `openBenchmark` shows them inline with a chain `6_book_trend_benchmark ← N retained decisions ← 5_scenario ← 1_source_document`. Makes DEMO_QA #1's "show, don't say" true.
- **D26** Two D25 items reverted per user: (a) **Ask the book** back to the two-surface **toggle** (Workbench panel · Native Genie) — the merged single-panel layout "went too far"; `genieQuery`/`GENIE_PENDING` kept so the Agents inline ask + dashboard follow-ups still work. (b) **Presenter tab removed** from the app (nav item, `renderPresenter`/`mdToHtml`, `GET /api/presenter`, `PRESENTER_MODE`, `app/presenter/` all deleted) — the run-sheet + Q&A instead live in the **Google Doc** (single source `docs/DEMO_RUNSHEET.md` + `docs/DEMO_QA.md`), linked from the actuarial-workbench hub next to the app. The Agents actionability and Genie hardening from D25 stay.
- **D25** Presenter-readiness pass. (a) **DEMO_RUNSHEET.md rewritten** for a cold presenter — every beat is GO/DO/SAY/IF-ASKED (≤4 lines; beat 5 six), reordered to the broker's process (receive→verify→check math→challenge→decide→refresh→learn), minute budgets + cut order + audience framing header, SAY lines carry no platform/object words (those live in Q&A), pre-computed bold numbers. (b) **DEMO_QA.md** — 19 persona Q&As (actuary/platform/exec), cross-linked to beats; answers checked against DEMO_READINESS caveats (e.g. Q9 notes the live slider preview is client-side, reconciled on save). (c) **Presenter tab** in the app (`renderPresenter` + `GET /api/presenter`, gated by `PRESENTER_MODE`; pinned bottom-left) renders both docs, single-sourced from `docs/` and staged into `app/presenter/` at deploy (committed generated copies, like the pipeline mirror). (d) **Agents made actionable** (were display-only): Reviewer → "Open the flagged renewal", Deal Summariser → inline "Draft a summary now" (FMAPI), Genie → inline ask box + "Open full Ask the Book". (e) **Genie hardened**: `execute_message_attachment_query` returns an empty `statement_response` → now guarded (skip to `get_message_attachment_query_result`) instead of relying on an exception; verified returns text+SQL+rows.
- **D24** Demo file now **mirrors the original example's structure** (user chose "full mirror" after the similarity comparison). `data/make_carrier_file.py` adds a **Benefits Overview** tab (plan × benefit lines) and a **Detailed Rates** sheet (Current + Renewal per plan × tier, with subs + Monthly Plan Cost) — synthetic, **generic plan names** (no real product brands). `parse_detailed` wired into `ingest_pipeline.py` populates `1_detailed_rates` — now **24 rows** (12 current + 12 renewal); was 0, so this **closes review finding D-3**. Carrier template `expected_tabs` → 6. The hero still reproduces **26.9491%** at 17/17 extraction — the new sheets don't feed the build-up (current premium still read from the Rate Development field), so the regression is preserved. Detailed rates are per-contract and illustrative (not reconciled to the PMPM build-up). Demo file: 4 tabs → 6.
- **D23** App polish round: (a) **Open workspace** now shows an immediate loading state (`openDoc` renders a spinner before the governed-function round-trip — was "looks like it does nothing"); (b) **Ask the book** merged into one panel — the native Genie embed sits inline below the quick-ask box, no toggle/extra click; (c) **Dashboard redesigned** (`data/create_dashboard.py`) — themed palette (not all blue), KPI counters, ask-vs-negotiated grouped bars (amber vs green), value-negotiated by carrier, book-trend with padder color-mapping, renewals-by-band pie, chronological claims line (temporal axis); linked the **Genie space into the dashboard** via `uiSettings.genieSpace` (in-dashboard Ask-Genie button) and added example follow-up questions on the app Dashboard screen (jump to Ask-the-book). Dashboard updated in place (same id 01f19d737…). All SQL tested via warehouse before publish.
- **D22** Demo files staged in the Volume (`landing/demo_files/`) so an SA running the demo can pull them **without cloning the repo** — download via Catalog Explorer or `databricks fs cp`, then drop into the app. `data/stage_demo_files.py` regenerates + uploads the four synthetic files; wired into `deploy.sh` (step 4) and the reset Job (restages on every reseed). Ingestion Drop card links straight to the folder; DEMO_RUNSHEET says "pull these to your computer first". Repo `data/` remains the fallback for those who cloned.
- **D21** Added a **Learn** panel (nav after Benchmarks) — the how-it-works layer: nine renewal activities (broker voice) → how the workbench does each → deep links into the live governed objects. Additive only (no schema/table/math changes). Backend `GET /api/learn` builds cards from `LEARN_CARDS` (single copy source, mirrored in `docs/LEARN_PANEL.md`) with links from the shared `_workspace_links()` helper (env-driven host + catalog/schema + job-id-by-name; no hardcoded hosts). Frontend: journey flow (3 group bands), one card open at a time, inline-SVG schematics (no chart libs), in-app anchors for in-app targets + new-tab external links. Readability rule enforced: card layers 1–2 carry no object names (verified) — those live only in the "See it live" links. "❓ how does this work?" glyphs on Book/Workspace/Benchmarks/Ask open Learn at nodes 2/3/8/9 via `LEARN_ANCHOR`.
- **D20** In-app **Reset demo** button (user asked for a button instead of a CLI pre-flight; chose the bulletproof full-reseed over a fast soft-reset). Implemented as a serverless Job `hb_reseed` (`[hb-renewal] demo reset (reseed)`, `jobs/run_reseed_job.py` → regenerate demo files → `seed_book` (runs `build_schema`, ingests hero, seeds historical+governance) → `generate_reviewer`), triggered by `POST /api/reset` (`jobs.run_now`) with `GET /api/reset/status` polling; button on the Daily view shows a spinner and refreshes on success. App SP granted `CAN_MANAGE_RUN` on the job via the DAB; the job runs as the bundle deployer (has schema CREATE). Required refactoring `build_schema`/`seed_book`/`generate_reviewer` to env-based auth (`profile = HB_PROFILE or None`) so they work under the job's ambient identity; `deploy.sh` now exports `HB_PROFILE`.
- **D19** Local regression recorded: `tests/test_regression.py` 2/2 PASS against `data/reference_exhibit.json` (reference exhibit reproduced to 4dp). Recorded in `docs/REVIEW/SMOKE_RESULTS_ADDENDUM.md` with reference-exhibit language only (no confidential identifiers). Tests `skipif` when the gitignored fixture is absent, so a public clone reports 2 skipped — accepted.
- **D18** Deploy consolidation: added `deploy.sh` (fail-fast, runs the numbered README steps in order incl. the idempotent Genie deploy + dashboard + app + bundle). README now says "one command (`./deploy.sh`) or the steps below". Full orchestrator-Job refactor deferred to V2 (ROADMAP).
- **D17** Genie deploy fragility contained: single idempotent `data/deploy_genie_agent.py` (genie-rooms builder + `databricks api post`/`patch`) — finds the space by title, updates if present / creates if absent (verified re-runnable twice with no duplicate; exactly one "HB Renewal Book" space remains), grants the app SP CAN_RUN, and auto-writes the space id to `data/genie_space_id.txt` and `app/app.yaml`. The old create-only `data/create_genie_space.py` was removed (superseded); the SDK/REST/CLI `serialized_space` failure + retest condition are documented in `docs/GENIE_SETUP.md` as known debt. Supersedes the create side of D12/D3.
- **D16** Reproducibility fix (closes review finding D-1): `mv_renewal_actions` and `mv_claims_experience` are now created by `data/build_schema.py` (were previously ad-hoc, created by no committed code, so a clean rebuild broke Genie/the dashboard). They are plain SQL views named `mv_*`, NOT UC metric views — promotion is on the roadmap. Corrects D12's "created in-schema" claim.
- **D15** App-layer verification recorded (the WP7/WP8 evidence gap): the full app loop, the four in-app Genie questions + a stateful follow-up, and live-drop timings were exercised against DEV and recorded in `docs/REVIEW/DEMO_READINESS.md` + `SMOKE_RESULTS_ADDENDUM.md` (HTTP status, warehouse statement ids, row/version reads — not code citations). Verification artifacts cleaned by a full reseed afterward.
- **D14** A personal email address was hardcoded as a Genie space `parent_path` in the old create-only script — PII that reached the public repo. Fixed to derive the parent path from the current user at runtime; the public history was rewritten to a single sanitised commit (force-push). Accepted-public risk logged in D13.
- **D13** Repo IS public at github.com/wryszka/hb-renewal-workbench (supersedes D8's "stays local"), explicitly directed with a sanitised-only push. Publication safety audit run against a fresh clone (`docs/REVIEW/NAME_SCAN.md`): zero real-party-name hits; no confidential files in tree or history; single commit. **Accepted public risk (explicitly):** workspace host `fevm-lr-dev-aws-us`, catalog `lr_dev_aws_us_catalog`, warehouse `a3b61648ea4809e3`, Genie space id, and dashboard id appear in committed config — none are secrets (access requires authenticated, granted principals), and they were already in the README.

## Build-time decisions
- **D7** No real broker/carrier/client names in any committed or deployed asset — this includes removing the real broker and carrier names from all committed docs and code comments. The app reads as a generic H&B broking workbench. Confidential base-data (corpus, reference exhibit, planning docs) stays local + gitignored, never committed/deployed.
- **D8** Repo not pushed to public GitHub (confidential adjacency); stays local unless explicitly directed with a sanitised-only push.
- **D10** The book is pre-seeded so the app is never empty: ~49 historical documents (summary-only: `1_source_document` + `5_scenario`) plus the hero document with full line-items in the four source tables. The hero is loaded **through the ingestion module** (same code path as the live Job), so the seed itself proves the pipeline. Live-drop demo beats use the broken file (quarantine), v2 (supersession) and month13 (reforecast); re-dropping the hero supersedes gracefully.
- **D11** `0_carrier_template.field_map` stored as a JSON string (not a UC MAP) for INSERT simplicity; `expected_tabs` as `ARRAY<STRING>`.
- **D9** File-arrival trigger wired on the UC Volume `inbox/` **plus** a one-click "run now" fallback in the app, so the live-drop beat never stalls on trigger latency (~1 min poll).
- **D12** Genie: metric/consumption views (`mv_renewal_actions`, `mv_claims_experience`) created in-schema; the "Ask the book" panel uses the SDK Genie **Conversation API** against a space id in `HB_GENIE_SPACE_ID`. `deploy_genie_agent.py` attempts programmatic space creation via `w.genie.create_space`; because `serialized_space` shape is version-specific, RESOLVED: creation works via the genie-rooms GenieSpaceBuilder + `databricks api post` (see data/create_genie_space.py); space 01f19d63… created, app SP granted CAN_RUN, HB_GENIE_SPACE_ID set — in-app panel live. Raw SDK create_space/REST/CLI all fail on serialized_space encoding.
